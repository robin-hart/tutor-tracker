/create-subagent i need a agent that runs every available frontend realted test from the project. In the /frontend folder a vue js with typescript project can be found. Via npm the agenrt should run "npm run test" (unit and integration tests) as well as "npm run e2e" (end to end tests, includoing smoke tests with playwright). After the agent received the tests results, he should fix the tests. Especially with the end to end test the agent should includiong a live view of the website and comunicate with teh vue js debhugger to find the porblems. Changing a test instead of fixing a problem is okay if it is inevitable. The agent should be aware of the requirements to run a end to end test: the backend must be running ("mvn springboot:run" in the /api folder) and the frontend must be running as well ("npm run dev" in the /frontend folder).